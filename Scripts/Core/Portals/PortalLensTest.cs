using UnityEngine;

public class PortalLensTest : MonoBehaviour
{
    [SerializeField] private PortalLensManager lensManager;
    [SerializeField] private Transform arrow;

    private void Start()
    {
    //    PortalLens mouseLens = lensManager.CreateMouseLens();
    //    mouseLens.SetDiameter(250f);

    //    PortalLens arrowLens = lensManager.CreateTargetLens(arrow);
    //    arrowLens.SetDiameter(400f);
    }
}